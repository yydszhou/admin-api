<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xugang.ai.mapper.PermissionMapper">

    <!-- 可复用的SQL片段 -->
    
    <!-- 基础权限查询字段（带表别名） -->
    <sql id="permissionColumnsWithAlias">
        p.id, p.permission_code, p.permission_name, p.module, p.description, p.create_time, p.update_time, p.is_deleted
    </sql>
    
    <!-- 基础权限查询字段（不带表别名） -->
    <sql id="permissionColumns">
        id, permission_code, permission_name, module, description, create_time, update_time, is_deleted
    </sql>
    
    <!-- 权限基础查询条件 -->
    <sql id="permissionBaseCondition">
        p.is_deleted = 0
    </sql>
    
    <!-- 根据用户ID查询权限列表 -->
    <select id="selectPermissionsByUserId" resultType="com.xugang.ai.entity.Permission">
        SELECT DISTINCT <include refid="permissionColumnsWithAlias"/>
        FROM permissions p
        INNER JOIN role_permissions rp ON p.id = rp.permission_id
        INNER JOIN roles r ON r.id = rp.role_id
        INNER JOIN user_roles ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND <include refid="permissionBaseCondition"/>
          AND r.is_deleted = 0
    </select>
    
    <!-- 查询权限列表（支持关键词搜索） -->
    <select id="selectPermissionList" resultType="com.xugang.ai.entity.Permission">
        SELECT <include refid="permissionColumnsWithAlias"/>
        FROM permissions p
        <where>
            <include refid="permissionBaseCondition"/>
            <if test="keyword != null and keyword != ''">
                AND (p.permission_name ILIKE CONCAT('%', #{keyword}, '%')
                     OR p.permission_code ILIKE CONCAT('%', #{keyword}, '%')
                     OR p.module ILIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY p.module, p.id
    </select>
    
    <!-- 根据权限ID统计数量 -->
    <select id="countByPermissionId" resultType="long">
        SELECT COUNT(*) 
        FROM permissions 
        WHERE id = #{permissionId}
    </select>
    
    <!-- 更新权限启用状态 -->
    <update id="updatePermissionEnabled">
        UPDATE permissions
        SET is_deleted = #{isDeleted},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{permissionId}
    </update>

</mapper>